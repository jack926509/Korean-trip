{
  "name": "korean-trip-guide",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate-data.mjs",
    "test": "node --test tests/*.test.mjs",
    "build": "node scripts/build.mjs",
    "check": "npm run validate && npm test && npm run build"
  },
  "engines": { "node": ">=20" }
}
