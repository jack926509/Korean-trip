# 第一版本機獨立驗收

驗收日期：2026-09-19（Asia/Taipei）
實作：Sol；獨立驗收：Astra。驗收者未修改應用程式碼。
結論：本機技術與既有資料保真驗收通過；仍待使用者親自走過核心流程。未部署網站，未將旅遊筆記視為官方確認資訊。

## 範圍與實跑結果

依據 `PLAN.md`、`docs/food-inventory.md` 及專案規則驗收。餐廳比對對象為本次既有來源盤點，不代表重新向 Google Sheets 或各店官網查證。

`npm run check` 最終由 Astra 獨立執行，退出碼 0：

```text
資料驗證通過：7 天行程、17 筆餐廳與全部關聯有效。
tests 8 / pass 8 / fail 0
建置完成：dist（6 個頁面）
```

Google Chrome（本機正式安裝版）搭配 Playwright，在 `http://127.0.0.1:4173/` 實際載入頁面：

| 頁面 | 375 px 手機 | 1440 px 桌面 |
| --- | --- | --- |
| 首頁 | 通過 | 通過 |
| 每日行程 | 通過 | 通過 |
| 景點 | 通過 | 通過 |
| 美食 | 通過 | 通過 |
| 購物 | 通過 | 通過 |
| 實用資訊 | 通過 | 通過 |

12 組頁面均有主標題、無整頁水平溢出；最後一輪 `consoleErrors: []`，無 JavaScript 頁面錯誤。另實看首頁、行程、景點、美食、購物、資訊截圖；修正後桌面 DAEJEON 文字位於票卡內，手機首頁文字、按鈕與底部導覽正常。日期列、首頁每日卡片於手機採可水平捲動容器，不是整頁溢出。

## 資料與操作驗證

- 程式逐字比對 17 筆餐廳的 `rawName`、`rawHours` 與來源儲存格範圍，全部符合既有盤點。山清兩筆分開，`11:300` 異常原文與跨午夜時段均保留。
- 7 天日期為 9/24 至 9/30，星期正確；逐一點首頁入口與行程日期頁籤，均顯示對應日期。
- 無垢屋精確連至 `food-015`，新村豆腐鍋精確連至 `food-002`；實際點擊後到達指定卡片。泛稱豬湯、泡菜鍋、烤肉未擅配店家，兩筆 uglybakery 未合併。
- 未知時間顯示待定；麵包店 08:00～23:00 保留為原營業筆記，不作活動停留時間。住宿僅保留來源列出的 9/24、9/27；大田與西大田保持區別。
- 美食中文與含空白韓文搜尋通過；搜尋山清顯示 2 筆。篩選西 4 筆、新堂站 2 筆、烤肉 4 筆；景點古蹟 1 筆、商店超市 1 筆。
- 景點、美食、購物的不存在關鍵字均出現空結果；點空結果區的清除按鈕可恢復完整清單。上方清除按鈕亦有綁定。
- 未知 `?day=unknown` 會回到第一天；日期頁籤左右方向鍵可切換，焦點跟隨。
- 餐廳可展開原表筆記、餐點和來源；非網址出處「只要有空去吃的」可見。未確認地址不生成無效地圖／官網按鈕。
- 正式購物商品為 `[]`，顯示待補；7 間商店與商品區分開。未產生商品、價格、庫存或購買數量。
- 在 `/tmp` 獨立副本改 `food-015` 店名與時段後重建，餐廳頁及行程餐廳連結名稱一致，餐廳時段更新；正式資料未改。
- 在獨立副本加入含品牌、規格、數量的測試商品，建置後顯示商品，移除待補空狀態；以真實 Chrome 開啟確認。測試商品未混入正式資料。
- 副本店名包含 `<img>`、商品包含 `<script>` 時均顯示為文字，未生成注入元素或跳出對話框；`javascript:` 來源 URL 不生成可執行連結。首頁試算表 URL 同樣限制 HTTP(S)。

## 審查發現與修正複驗

以下均已由 Sol 修正，Astra 複驗通過；行號為驗收時的修正後位置。

| 原問題 | 修正位置 | 修正與複驗 |
| --- | --- | --- |
| 麵包店營業時間被當成活動時間 | `data/days.json:11` | 活動時間改 null，原時段留備註。 |
| 山清 2 店誤複製另一店的 navermap 備註 | `data/restaurants.json:14` | 移除無來源筆記，保留分店疑義。 |
| 空結果的第二顆清除按鈕沒有事件 | `src/app.js:33` | 綁定所有清除按鈕，三分類頁實點通過。 |
| 購物資料非空時仍只有待補狀態 | `scripts/build.mjs:61` | 依集合渲染商品，副本重建與瀏覽器驗證通過。 |
| 首頁缺航班住宿摘要、分類頁缺適用篩選與日期關聯 | `scripts/build.mjs:32`、`scripts/build.mjs:46`、`scripts/build.mjs:55` | 已補摘要、篩選與已排入日期。 |
| 摘要欄位誤稱 raw、文字型推薦出處未顯示 | `data/sources.json:12`、`scripts/build.mjs:54` | 摘要明確為 rawSummary，透過 recordRef 關聯原始餐廳欄位；原始盤點繼續保留，文字型出處可見。 |
| Date.parse 接受不存在的 2/31 | `scripts/validate-data.mjs:35` | 增加日期 round-trip 比對與回歸測試。 |
| 未知日期參數使全部日期隱藏、tab 缺方向鍵操作 | `src/app.js:48`、`src/app.js:58` | fallback 及左右方向鍵實測通過。 |
| favicon 404、首頁票卡城市字超出範圍 | `scripts/build.mjs:25`、`src/styles.css:3` | favicon 已載入；最後六頁 console 零錯誤，票卡文字邊界檢查通過。 |

## 證據與後續

本機測試腳本與截圖留於 `/tmp`，不提交大型截圖：

- `/tmp/korean-acceptance.cjs`：六頁、兩尺寸、核心互動。
- `/tmp/korean-acceptance-results.json`：12 組頁面結果與空 consoleErrors。
- `/tmp/korean-extra.cjs`、`/tmp/korean-final-extra.cjs`：篩選、首頁入口、鍵盤與修正後版面。
- `/tmp/korean-mutation.mjs`：資料更新、副本商品與 escaping/URL 測試。
- `/tmp/korean-final-home-mobile.png`、`/tmp/korean-final-home-desktop.png`：最終首頁畫面。
- `/tmp/korean-375-*.png`、`/tmp/korean-1440-*.png`：最後一輪六頁完整截圖。

仍未官方查證的內容包括餐廳營業、價格、假期休業、訂位方式、店名與分店疑義、景點票價／開放時間、航班資訊、鐵路班次及轉乘、住宿完整名稱與入住範圍。網站已明示待確認；這次驗收確認的是資料沒有擅自補成事實，以及網站能正確呈現與操作。

本機交付尚餘使用者體驗確認：首頁選日期 → 查看行程 → 開啟無垢屋或新村豆腐鍋 → 搜尋／篩選美食 → 查看購物。公開部署與遠端 CI 結果另由主代理確認，不包含在本機驗收通過聲明。
